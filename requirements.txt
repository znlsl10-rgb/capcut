# CapCut Auto-Edit Agent — runtime dependencies
#
# 무거운 의존성(오디오 분석/받아쓰기/드래프트 생성)은 실제 편집 실행 시에만 필요합니다.
# 순수 로직 유닛 테스트는 아래 패키지 없이도 동작합니다.

# 캡컷(CapCut) draft(초안) 프로젝트 파일 생성
pyCapCut>=0.0.3
pymediainfo>=6.1.0        # pyCapCut이 소재 길이/해상도 추출에 사용

# 오디오 비트/드롭 분석
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0

# 가사 자동 받아쓰기 (둘 중 하나 설치)
#   1) faster-whisper (권장, 빠르고 가벼움)
#   2) openai-whisper
faster-whisper>=1.0.0

# 설정 파일 파싱
PyYAML>=6.0

# 송북(곡별 정답 가사) 엑셀 읽기
openpyxl>=3.1.0

# 로컬 웹앱 (talkcut: 토킹 영상 편집기)
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9   # 파일 업로드


# threadscout (해외 스레드 분석 → 쿠팡 파트너스 글)
#   수집/분석 엔진은 표준 라이브러리만 사용합니다(Apify·쿠팡 API 모두 urllib).
#   웹 UI 는 위의 fastapi/uvicorn 을 그대로 씁니다.
deep-translator>=1.11.0   # 선택: 해외 글 자동 번역(없으면 원문 유지)
